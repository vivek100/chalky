# Moss project credentials (indexing scripts + browser embedded retrieval)
MOSS_PROJECT_ID=
MOSS_PROJECT_KEY=
MOSS_INDEX_NAME=moss-demo-site

# Minimax LLM agent (tool-calling, structured decisions, TTS/STT)
MINIMAX_API_KEY=
MINIMAX_MODEL=abab6.5-chat

# LiveKit voice transport (browser room client + agent session)
LIVEKIT_URL=
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# Unsiloed structured extraction (optional — context ingestion pipeline)
UNSILOED_API_KEY=
